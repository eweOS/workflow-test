name: Test Workflow

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/eweos/docker:master
    steps:
      - name: echo
        run: echo "Hello!"
      - name: list installed packages
        run: pacman -Q
